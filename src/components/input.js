import React from 'react';

const Input = (word="Word Type") => {
    return (
        <div className="input">
            <input />
            <label>{word}</label>
        </div>
    )
}

export default Input;